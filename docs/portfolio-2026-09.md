# September 2026 portfolio refresh

Homepage only: original project applications, logo image bytes, domain configuration and contact destination are retained.

Design: dark ink / silver-blue studio identity, sans-serif typography, asymmetric website covers, project directory, a dedicated SafiStudios product showcase, sample switcher, accessible full-screen image dialog, responsive navigation and optional restrained canvas motion. Existing project links and legacy section anchors remain available.

SafiStudios is the public-facing name requested for the Cosmos product. The examples are illustrative images based on the user's supplied application screenshot, populated with fictional data. They do not advertise route maps, dispatch timelines or features from the discarded initial concepts.

Image provenance: the built-in image-generation tool edited the supplied actual app screenshot. Coffee prompt: preserve sidebar, toolbar, mountain banner and two-column module cards; populate Maple & Bean orders, inventory, staff schedules and daily close. Pest prompt: preserve that same UI; populate Cedar Pest Control jobs, pipeline, scheduling and invoices. Final web assets: assets/showcase/coffee-dashboard.webp and pest-dashboard.webp. The two original generated files remain separately available in the conversation. Elizabeth Aven cover photograph is copied from the existing public project repository (couple1.jpg); the other cover is an original CSS/SVG illustration, not a site screenshot.

Validation: tools/check-portfolio.cjs checks destinations, image paths, contact endpoint, sample switching, keyboard tabs, full-screen dialog, filters, menu, motion and inquiry preselection. Run with Node and jsdom. Browser capture of a client site was denied by the browser approval policy, so no screenshot-based visual verification is claimed.
